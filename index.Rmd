# Datasets

```{r echo=FALSE, results='asis'}

files <- sort(list.files(path = "./data", pattern = "*", full.names = TRUE))

for (file in files) {
  cat(sprintf("- [%s](%s)\n", basename(file), file))
}

```

*Last Updated: `r format(Sys.time(), "%b %d, %Y %l:%M %p")`*
